import { colors } from '../../constants/colors'

import styled from 'styled-components'

export const Container = styled.div`
  height: 60px;
  background-color: ${colors.dark.bgBase};
  padding: 0px 25px;
  color: ${colors.dark.fontBase};
`
